# OpenWebUI 配置
OPENWEBUI_DOMAIN=your_openwebui_domain # OpenWebUI 域名，例如 https://chat.example.com
OPENWEBUI_API_KEY=your_api_key # OpenWebUI API 密钥，用于获取 OpenWebUI 的模型列表

# 访问控制
ACCESS_TOKEN=your-access-token-here # 用于登录 Monitor 页面
API_KEY=your-api-key-here # 用于向 Monitor 发送请求的鉴权

# 价格配置（可选，元/百万tokens）
# DEFAULT_MODEL_INPUT_PRICE=60  # 模型默认输入价格
# DEFAULT_MODEL_OUTPUT_PRICE=60  # 模型默认输出价格
# DEFAULT_MODEL_PER_MSG_PRICE=-1 # 模型默认每条消息价格，-1 表示按 token 计费
# INIT_BALANCE=0 # 用户的初始余额，可选

# PostgreSQL 数据库配置（可选，如果使用外部数据库，请配置以下环境变量）
# POSTGRES_HOST=
# POSTGRES_PORT=
# POSTGRES_USER=
# POSTGRES_PASSWORD=
# POSTGRES_DATABASE=